---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: logstashfilters.logstash.vkiedrowski.de
spec:
  group: logstash.vkiedrowski.de
  names:
    kind: LogstashFilter
    listKind: LogstashFilterList
    plural: logstashfilters
    singular: logstashfilter
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LogstashFilter is the Schema for the logstashfilters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LogstashFilterSpec defines the desired state of LogstashFilter
            properties:
              data:
                type: string
              order:
                default: 50
                type: integer
            required:
            - data
            - order
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

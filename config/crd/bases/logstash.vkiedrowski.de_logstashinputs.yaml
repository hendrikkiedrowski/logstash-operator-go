---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: logstashinputs.logstash.vkiedrowski.de
spec:
  group: logstash.vkiedrowski.de
  names:
    kind: LogstashInput
    listKind: LogstashInputList
    plural: logstashinputs
    singular: logstashinput
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LogstashInput is the Schema for the logstashinputs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LogstashInputSpec defines the desired state of LogstashInput
            properties:
              data:
                type: string
              service:
                description: LogstashInputService defines the desired state of LogstashInputService
                properties:
                  name:
                    type: string
                  port:
                    type: integer
                  type:
                    type: string
                required:
                - name
                - port
                - type
                type: object
            required:
            - data
            - service
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

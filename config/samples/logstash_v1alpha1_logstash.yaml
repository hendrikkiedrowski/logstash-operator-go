apiVersion: logstash.vkiedrowski.de/v1alpha1
kind: Logstash
metadata:
  name: logstash-sample
spec:
  configReload:
    automatic: true
    interval: 3
  replicaCount: 1
  storage:
    storageClassName: local-path

